# we can put any type on a variable
# the declaration is done automatically
# it is case sensitive my_var is different of my_Var
# we have to use = sign to attribute some value

a = b = c = 10

a, b, c = 1, 2, 3

# id() it returns the local memoty where the value is stored
# python keywords
# False      await      else       import     pass
# None       break      except     in         raise
# True       class      finally    is         return
# and        continue   for        lambda     try
# as         def        from       nonlocal   while
# assert     del        global     not        with
# async      elif       if         or         yield